# U1’s Birthday Blind Box Challenge 🎁 — README

## What you need

* A computer with **Chrome** or **Safari**.
* No installation required.

## Files (two versions)

* **Classic version (simple UI)**

  * `index.inline.html`
  * `app.inline.js`
  * `style.cute.css`
* **Probability version (shows live chances & a small curve)**

  * `index.inline.prob.html`
  * `app.inline.prob.patched.js`
  * `style.cute.css`

> You can use either version. Both work offline.

## How to start

1. Download the game files into a single folder:

   * [index.inline.html](sandbox:/mnt/data/u1_blind_box_game/index.inline.html)
   * [app.inline.js](sandbox:/mnt/data/u1_blind_box_game/app.inline.js)
   * — or —
   * [index.inline.prob.html](sandbox:/mnt/data/u1_blind_box_game/index.inline.prob.html)
   * [app.inline.prob.patched.js](sandbox:/mnt/data/u1_blind_box_game/app.inline.prob.patched.js)
   * Theme: [style.cute.css](sandbox:/mnt/data/u1_blind_box_game/style.cute.css)
2. **Open the HTML file** by double-clicking:

   * Classic: `index.inline.html`
   * With probability: `index.inline.prob.html`

## How to play (quick loop)

1. Click **Roll!** to get a box number (1–16).
2. Choose **Buy** (continue) or **Re-roll** (keep number in the pool).
3. **Guess** the item first (don’t open your physical box yet).
4. Then **open** the box in real life and click the matching item on screen.

   * Your on-screen balance will update automatically.
5. You’ll return to the home screen to roll again. Previously opened numbers are removed.

### If you chose the **Probability version**

* A right-side panel shows your **current chance** to succeed and a small **curve** of chances at nearby levels.
* It updates as you play. (No action needed.)

## Tips

* Re-roll only keeps the number in the pool (nothing is lost).
* Make your best guess before opening to earn a small bonus when correct.
* Have fun and celebrate each pull!

## Troubleshooting

* If the page shows only a header and nothing else, make sure you opened the **inline** HTML (`index.inline.html` or `index.inline.prob.html`) and that **style.cute.css** is in the same folder.
* Try another browser tab or refresh the page.

Enjoy and Happy 1126!
